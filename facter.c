//created to be fast
static double evalSpeedDifference( double boatSpeed, double windSpeed, double windAngle );
static double evalTrueWindSpeed( double windSpeed, double windAngle );
static double[][] initPolarDiagram( void );

void main( int )
{
//init the bus
//listen to the bus
//parce the message
//evaluate the result
}
